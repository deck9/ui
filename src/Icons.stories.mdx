<!--- Icons.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import IconCollection from "./Icon/IconCollection.vue";
import { icons, brands } from "@/index";

<Meta title="Reference/Icons Overview" />

# Icons used in Deck9 UI

This page gives an overview of all icons used in Deck9 UI.

export const Primary = (args) => ({
    components: {IconCollection},
    setup() {
      return {args, icons: [...icons, ...brands]};
    },
    template: '<icon-collection v-bind="{icons}" />',
});

<Story name="All">
    {Primary.bind({})}
</Story>

