<!--- Icons.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import IconCollection from "./Icon/IconCollection.vue";
import { icons } from "@/index";

<Meta title="Reference/Icons Overview" />

# Icons used in BotReach UI

This page gives an overview of all icons used in BotReach UI.

export const Primary = (args) => ({
    components: {IconCollection},
    setup() {
      return {args, icons: icons};
    },
    template: '<icon-collection v-bind="{icons}" />',
});

<Story name="All">
    {Primary.bind({})}
</Story>

